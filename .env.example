# Zkopiruj tento soubor jako .env a vypln skutecne hodnoty
# Soubor .env je v .gitignore — nikdy ho necommituj

# PostgreSQL heslo pro databazi
DB_PASSWORD=changeme

# PostgreSQL heslo pro root uzivatele (pouziva se jen pri inicializaci)
POSTGRES_PASSWORD=changeme
